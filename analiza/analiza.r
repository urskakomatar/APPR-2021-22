# 4. faza: Napredna analiza podatkov

#za leto 2019

library(ggalt)
#skupine <- data.frame(Gospodarske_značilnosti$bdppc,Gospodarske_značilnosti$indeks);
#diagram.skupine = function(podatki, oznake, skupine, k) {
#  podatki = podatki %>%
#    bind_cols(skupine)%>% rename(Država = ...4)
  
#  d = podatki %>%
 #   ggplot(
  #    mapping = aes(
   #     x = bdppc, y = indeks, color = Država
#      )
 #   ) +
  #  geom_point() +
   # geom_label(label = oznake, size = 2) +
    #scale_color_hue() +
#    theme_classic()
  
#  for (i in 1:k) {
 #   d = d + geom_encircle(
  #    data = podatki %>%
   #     filter(Država == i)
#    )
#  }
#  d
#}


